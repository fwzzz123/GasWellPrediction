<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.proj.mapper.WellLogMapper">


    <!-- 定义SQL语句 -->
    <select id="getWellLogIdByFileName" resultType="java.lang.String">
        SELECT id FROM well_log WHERE file_name = #{fileName} LIMIT 1
    </select>

    <insert id="insert" parameterType="com.proj.entity.po.WellLogPO">
        INSERT INTO well_log (id, well_id, start_depth, end_depth, step, create_time, update_time)
        VALUES (#{id}, #{wellId}, #{startDepth}, #{endDepth}, #{step}, NOW(), NOW())
    </insert>

    <select id="getWellLogByFileName" resultType="com.proj.entity.po.WellLogCurveMappingPO">
        SELECT *
        FROM well_log_curve_mapping
        WHERE file_name = #{fileName}
    </select>


</mapper>
